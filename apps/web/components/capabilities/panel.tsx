// Shared shell for the animated "result" panels. The outer capability card
// provides the border, rounding, and background; the panel just fills its
// column's width.
export const PANEL_CLASS = "w-full";
